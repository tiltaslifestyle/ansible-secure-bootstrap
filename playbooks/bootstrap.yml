---
- name: Prepare servers 
  hosts: staging # Options: staging, prod, all
  become: true
  
  roles:
    - role: common # Base utilities and Chrony
    - role: security # Hardening (SSH, UFW, Fail2Ban)
    - role: docker # Container runtime environment
